# 1. 파이썬 3.9 버전이 깔린 가벼운 리눅스를 기본 바탕으로 사용합니다.
FROM python:3.9-slim

# 2. 컨테이너 내부의 작업 디렉토리를 /app으로 설정합니다.
WORKDIR /app

# 3. 내 컴퓨터의 재료 목록을 컨테이너 안으로 복사합니다.
COPY requirements.txt .

# 4. 필요한 라이브러리들을 설치합니다.
RUN pip install --no-cache-dir -r requirements.txt

# 5. 내가 짠 파이썬 코드를 컨테이너 안으로 복사합니다.
COPY . .

# 6. 이 컨테이너가 실행될 때 파이썬 코드를 실행하라고 명령합니다.
CMD ["python", "transform_data.py"]
